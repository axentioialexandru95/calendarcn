---
title: Interactions
description: The composed starter bundle with optimistic move and resize updates, optional confirmation, and the built-in create sheet.
---

## Direct Editing Flow

<CalendarExample id="interactions" />

This version is the closest match for product integrations that want the
current `CalendarScheduler` starter behavior plus guardrails around
persistence.

- Uses optimistic local updates for move and resize
- Shows how to enable move confirmation without blocking every action
- Includes the built-in create sheet for richer event capture
