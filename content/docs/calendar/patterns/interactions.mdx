---
title: Interactions
description: Optimistic move and resize updates with optional confirmation and the built-in create sheet.
---

## Direct Editing Flow

<CalendarExample id="interactions" />

This version is the closest match for product integrations that need direct
manipulation plus guardrails around persistence.

- Uses optimistic local updates for move and resize
- Shows how to enable move confirmation without blocking every action
- Includes the built-in create sheet for richer event capture
